import logging
import socket
import channelsimulator
import utils
import sys
import math

partitionnum = int(math.ceil(len(self.data)/float(packetsize))
partitioncount = 0

def makePKT(self, data, packetnum, packetsize):
	for i in range(self.partitionnum):
		partitioncount = partitioncount + 1
		